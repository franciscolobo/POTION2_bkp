# comment lines are ignored;
# end directory names with '/' charactere whenever possible
# you can reference the potion_dir in the other parameters by writing "$potion_dir", when applicable (path to directories or files)

-=-=-= BASIC PARAMETER FOR LOCATION AND FILE NAME =-=-=-

potion_dir = /home/thieres/potion_rax/POTION2/POTION2              # path to this program, you can reference it in the parameters below by writing "$potion_dir" when applicable (path to directories or files).
-=-=-= PATH TO EXTERNAL PROGRAMS USED BY POTION =-=-=-

# type the path of the executable of each program below

codeml = /usr/bin/codeml                             # path to the Codeml executable of PAML package
consense = /usr/bin/consense                         # path to the Consense executable of Phylip package
muscle = /usr/bin/muscle                             # path to the MUSCLE executable
phipack = /usr/bin/Phi                               # path to the Phi executable of PhiPack
prank = /usr/bin/prank                               # path to the Prank executable
proml = /usr/bin/proml                               # path to the Proml executable of Phylip package
dnaml = /usr/bin/dnaml                               # path to the dnaml executable of Phylip package
seqboot = /usr/bin/seqboot                           # path to the Seqboot executable of Phylip package
trimal = /usr/bin/trimal                             # path to the Trimal executable
mafft = /usr/bin/mafft                               # path to the Mafft executable
pagan = usr/bin/pagan                                # path to the pagan executable
phyml = /usr/bin/phyml                               # path to the phyml executable
clustalo = /usr/bin/clustalo                         # path to the Clustal Omega executable
raxml = /usr/bin/raxmlHPC-SSE3                       # path to the RAxML executable
-=-=-= PARAMETERS FOR INTERNAL USE =-=-=-

result_table = final_results.out                  # file with the final result in tsv format (tab spaced values).
result_uncertain = uncertain.out                  # file with the list of clusters where the null hypotesis can't be ignored by the positive evidence. Leave empty if you do not desire to produce this list
result_positive = positive.out                    # file with the list of clusters with evidence of positive selection.

summary = summary.out
log_file = final_log.log                          # log file name
log_path = $potion_dir/log/                       # path where log file will be created
